# 🔐 Настройка аутентификации

## 📋 Что было добавлено:

### 1. Компоненты:
- **AuthModal.tsx** - Модальное окно для логина/регистрации
- Интеграция с кнопками "Try Aporto", "Try Aporto Now", "Get Started"

### 2. API Routes:
- **/api/auth/register** - Регистрация нового пользователя
- **/api/auth/login** - Вход существующего пользователя

### 3. База данных:
- **PostgreSQL (Neon)** - Облачная база данных
- Таблица `users` с полями: id, email, name, password, created_at

### 4. Навигация:
- ❌ Убрано: "Enhance" dropdown меню
- ❌ Убрано: "AI Photos" ссылка
- ✅ Оставлено: "Support" и кнопка "Try Aporto"

## 🚀 Установка:

### Шаг 1: Установите зависимости
```bash
npm install
```

### Шаг 2: Создайте файл .env.local
Создайте файл `.env.local` в корне проекта и добавьте:

```env
DATABASE_URL=postgresql://neondb_owner:npg_Pdsp2cZyj1zC@ep-twilight-truth-adopkfec-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
NEXTAUTH_SECRET=your-random-secret-key-here-change-this
NEXTAUTH_URL=http://localhost:3000
```

**ВАЖНО:** Замените `NEXTAUTH_SECRET` на случайную строку!

### Шаг 3: Инициализируйте базу данных
База данных автоматически создаст таблицу `users` при первом запросе.

### Шаг 4: Запустите сервер
```bash
npm run dev
```

## 🎯 Как это работает:

### Регистрация:
1. Пользователь нажимает на любую кнопку (Try Aporto, Try Aporto Now, Get Started)
2. Открывается модальное окно
3. Переключается на "Sign Up"
4. Вводит: имя, email, пароль
5. Данные отправляются на `/api/auth/register`
6. Пароль хешируется с помощью bcrypt
7. Пользователь сохраняется в PostgreSQL
8. Возвращается JWT токен

### Вход:
1. Открывается модальное окно
2. Режим "Sign In" (по умолчанию)
3. Вводит: email, пароль
4. Данные отправляются на `/api/auth/login`
5. Проверяется email и пароль
6. Возвращается JWT токен

### JWT Токен:
- Срок действия: 7 дней
- Содержит: userId, email
- Используется для аутентификации запросов

## 🔒 Безопасность:

- ✅ Пароли хешируются с bcrypt (10 раундов)
- ✅ JWT токены с секретным ключом
- ✅ Валидация данных на сервере
- ✅ Защита от SQL инъекций (параметризованные запросы)
- ✅ HTTPS обязателен для продакшена

## 📊 Структура БД:

```sql
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  email VARCHAR(255) UNIQUE NOT NULL,
  name VARCHAR(255) NOT NULL,
  password VARCHAR(255) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

## 🎨 UI/UX:

- Красивое модальное окно с градиентами
- Плавные анимации
- Валидация форм
- Сообщения об ошибках
- Переключение между Login/Register
- Адаптивный дизайн

## 🔧 Следующие шаги:

1. **Сохранение токена**: Добавьте localStorage или cookies для хранения JWT
2. **Защищенные роуты**: Создайте middleware для проверки токена
3. **Профиль пользователя**: Страница с данными пользователя
4. **Выход**: Кнопка logout
5. **Восстановление пароля**: Функция сброса пароля

## 📝 Примечания:

- База данных Neon автоматически масштабируется
- Бесплатный тариф: 0.5 GB хранилища
- Подключение через пул соединений
- SSL обязателен
