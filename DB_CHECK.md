# 🔍 Проверка подключения к базе данных

## ⚠️ ВАЖНО: Убедитесь что создан файл .env.local

Файл должен содержать:
```env
DATABASE_URL=postgresql://neondb_owner:npg_Pdsp2cZyj1zC@ep-twilight-truth-adopkfec-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
NEXTAUTH_SECRET=your-random-secret-key
NEXTAUTH_URL=http://localhost:3000
```

## 🧪 Тест подключения к БД

### Шаг 1: Запустите сервер
```bash
npm run dev
```

### Шаг 2: Откройте в браузере
```
http://localhost:3000/api/init-db
```

### Ожидаемый результат:
```json
{
  "success": true,
  "message": "Database initialized successfully",
  "usersCount": "0"
}
```

Если видите эту ошибку:
```json
{
  "error": "Failed to initialize database",
  "details": "...",
  "hint": "Check if DATABASE_URL is set in .env.local"
}
```

**Решение:**
1. Убедитесь что файл `.env.local` существует в корне проекта
2. Перезапустите сервер: `Ctrl+C` → `npm run dev`

## 🎯 Тест регистрации

### Через браузер:
1. Откройте http://localhost:3000
2. Нажмите "Try Aporto Now"
3. Переключитесь на "Sign Up"
4. Заполните форму:
   - Name: Test User
   - Email: test@example.com
   - Password: 123456
5. Нажмите "Sign Up"

### Ожидаемый результат:
- Модальное окно закроется
- В консоли браузера (F12) увидите: `Success: {user: {...}, token: "..."}`

### Если ошибка:
Откройте консоль браузера (F12) и посмотрите детали ошибки.

## 🔧 Проверка через curl:

### Регистрация:
```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "password": "123456"
  }'
```

### Вход:
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "123456"
  }'
```

## ✅ Что исправлено:

1. **Текст в инпутах теперь черный** (`text-gray-900 bg-white`)
2. **Автоматическое создание таблицы** при первом запросе
3. **Google Tag Manager** добавлен (GTM-5M3PTPFD)
4. **Улучшенная обработка ошибок** с подсказками
5. **Endpoint для проверки БД** (`/api/init-db`)

## 📊 Проверка данных в БД

После успешной регистрации, данные сохраняются в таблице `users` в вашей Neon PostgreSQL базе данных.

Вы можете проверить это в Neon Dashboard:
https://console.neon.tech/

## 🐛 Частые проблемы:

### 1. "Cannot find module '@neondatabase/serverless'"
**Решение:** 
```bash
npm install
```

### 2. "DATABASE_URL is not defined"
**Решение:** Создайте файл `.env.local` (см. выше)

### 3. Текст в инпутах не виден
**Решение:** Уже исправлено! Обновите страницу (Ctrl+R)

### 4. Google Tag Manager не работает
**Решение:** 
- Откройте консоль браузера (F12)
- Проверьте вкладку Network
- Должен быть запрос к `googletagmanager.com`
